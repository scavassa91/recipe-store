// Nesse contrutor as variaveis name e amount
// são criadas automaticamente pelo construor
// da mesma forma que no modulo recipe
export class Ingredient {
    constructor (public name: string, public amount: number) {}
}